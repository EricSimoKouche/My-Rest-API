# My RestFul API

Command to install it in a new machine
- npm init -y : to start a new node project. this creates a package in json an a context for installing packages that we can use in node

- npm install express : this line install the package __express__